# ROS2 入门笔记：从安装到第一个节点

最近开始学习 ROS2，作为机器人工程的学生，这是必须掌握的技能。这篇文章记录我从零开始安装和使用 ROS2 Humble 的过程。

## 安装环境

- 系统：Ubuntu 22.04 LTS
- ROS2 版本：Humble Hawksbill

## 核心概念

### 节点（Node）

节点是 ROS2 中最基本的执行单元。每个节点负责一个独立的功能模块，比如一个传感器驱动、一个运动控制器等。

### 话题（Topic）

话题是节点之间异步通信的方式。一个节点可以发布（publish）消息到话题，其他节点可以订阅（subscribe）这个话题来接收消息。

### 服务（Service）

服务是一种同步的请求-响应通信模式，适合需要确认结果的操作。

## 第一个 Publisher

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class MinimalPublisher(Node):
    def __init__(self):
        super().__init__('minimal_publisher')
        self.publisher_ = self.create_publisher(String, 'topic', 10)
        timer_period = 0.5
        self.timer = self.create_timer(timer_period, self.timer_callback)
        self.i = 0

    def timer_callback(self):
        msg = String()
        msg.data = f'Hello World: {self.i}'
        self.publisher_.publish(msg)
        self.i += 1

def main(args=None):
    rclpy.init(args=args)
    node = MinimalPublisher()
    rclpy.spin(node)
    node.destroy_node()
    rclpy.shutdown()
```

## 总结

ROS2 的学习曲线比想象中平缓，官方文档和教程质量很高。下一步计划学习 launch 文件和自定义消息类型。
